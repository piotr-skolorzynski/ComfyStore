import { Filters, PaginationContainer, ProductsContainer } from "../components";

const Products = () => {
  return (
    <>
      <Filters />
      <ProductsContainer />
      <PaginationContainer />
    </>
  );
};

export default Products;
